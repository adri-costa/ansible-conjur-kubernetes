all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_become: true

#Instalar em um host remoto
#all:
#  hosts:
#    conjur_server:
#      ansible_host: 192.168.23.154  # IP da VM onde o Conjur será instalado
#      ansible_user: noob           # Usuário que executará o Ansible
#      ansible_ssh_private_key_file: ~/.ssh/id_rsa  # Caminho da chave privada SSH
#      ansible_become: true            # Habilita sudo
